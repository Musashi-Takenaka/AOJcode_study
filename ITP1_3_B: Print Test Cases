! /usr/bin/env python
-*- coding: utf-8 -*-

"""
１つの整数 x を読み込み、それをそのまま出力するプログラムを作成
"""

##### while Trueで無限ループ
##### formatメソッドで変数を埋め込み
c = 0
while True:
    x = int(input()); c += 1
    if x == 0: break
    print("Case {}: {}".format(c, x))
