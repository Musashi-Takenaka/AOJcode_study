! /usr/bin/env python
-*- coding: utf-8 -*-

##### 入力
n=int(raw_input())
ans=''

##### 3で割り切れる or 桁のどこかに3を含むかの判定
for i in range(3,n+1):
    if i%3 == 0 or '3' in str(i):
        ans+=(' '+str(i))
print ans
